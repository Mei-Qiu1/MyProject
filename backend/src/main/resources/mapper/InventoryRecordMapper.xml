<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hospital.mapper.InventoryRecordMapper">

    <select id="sumOutQuantity" resultType="java.lang.Long">
        SELECT COALESCE(SUM(quantity), 0) FROM inventory_record WHERE type = 2
        <if test="startDate != null and startDate != ''">
            AND create_time &gt;= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            AND create_time &lt;= #{endDate}
        </if>
    </select>

    <select id="countDistinctDrugs" resultType="java.lang.Long">
        SELECT COUNT(DISTINCT drug_id) FROM inventory_record WHERE type = 2
        <if test="startDate != null and startDate != ''">
            AND create_time &gt;= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            AND create_time &lt;= #{endDate}
        </if>
    </select>

    <select id="countDistinctDepartments" resultType="java.lang.Long">
        SELECT COUNT(DISTINCT department) FROM inventory_record WHERE type = 2 AND department IS NOT NULL
        <if test="startDate != null and startDate != ''">
            AND create_time &gt;= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            AND create_time &lt;= #{endDate}
        </if>
    </select>

</mapper>
